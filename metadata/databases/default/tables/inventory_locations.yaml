table:
  name: locations
  schema: inventory
array_relationships:
  - name: inventory_items
    using:
      manual_configuration:
        column_mapping:
          id: location_id
        insertion_order: null
        remote_table:
          name: inventory_items
          schema: inventory
