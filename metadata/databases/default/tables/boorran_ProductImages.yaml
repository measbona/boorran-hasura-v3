table:
  name: ProductImages
  schema: boorran
array_relationships:
  - name: variants
    using:
      manual_configuration:
        column_mapping:
          productId: productId
        insertion_order: null
        remote_table:
          name: ProductVariants
          schema: boorran
